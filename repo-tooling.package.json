{
  "name": "dungeoncore-repo-tooling",
  "private": true,
  "scripts": {
    "commitlint": "commitlint --from=HEAD~1 --to=HEAD --verbose"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0"
  }
}
